<%= render 'hero'%>
<%# <%= render 'featured_products', { products: @featured_products } if @featured_products.try(:any?) %> 
<%# <%= render 'taxon', taxon: @taxon%> 
<%= render 'taxon_display', title: "French Tips", products: @french_tip_products, taxon: @basic_bundle_taxon %>
<%= render 'taxon_display', title: "Caps Two", products: @caps_products, taxon: @caps_taxon %>
<%= render 'collection', { title: "New Arrivals", products: @new_arrivals } if @new_arrivals.try(:any?) %>
<%= render 'collection', { title: "Shop By Category", products: @collection_products } if @collection_products.try(:any?) %>
<%= render 'categories', { title: "Shop By Category", taxon: @taxon} %>
<%= render 'collections_with_call_to_action', { products: @cta_collection_products[0..1] } if @cta_collection_products.try(:any?) %>
<%= render 'featured_product_banner', { product: @cta_collection_products[2] } if @cta_collection_products && @cta_collection_products.size > 2 %>

<%= render 'shared/navigation/taxonomies', taxon: @taxon %>

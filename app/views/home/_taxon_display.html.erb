<div class="products wrapper">
  <section class="products_section col-span-full md:col-span-9 lg:col-span-10">
    <div class="cat-header flex mb-6 flex-col">
      <h1 class="products__taxon-title mb-4 font-serif text-h3 lg:text-h2 text-center"><%= taxon.name %></h1>
      <p class="text-red text-center mb-4"><%= taxon.description %></p>
    </div>
    <%= render 'products/products', products: products, taxon: taxon %>
    <% unless params[:keywords].present? %>
      <% cache [I18n.locale, taxon] do %>
        <!-- Add content to be cached if needed -->
      <% end %>
    <% end %>
  </section>
</div>




